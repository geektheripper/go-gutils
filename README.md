# GeekTR Golang Utils

## Git

[hack_ssh](./git/hack_ssh)

find out which private key can login to a host, implemented by actually run `ssh -v` and parse the output, so it may not work for all ssh version
